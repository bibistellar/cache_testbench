测试0：小范围测试dcache的数据写入功能，写一个读取一个。
测试1：小范围测试dcache的数据写出功能，先全部写入，再随机读出。
测试2：测试dcache在数据未命中情况下的功能，先写入一组数据，然后用另一组数据进行覆盖，测试dcache能否正确地写回数据到内存中，同时正确返回待读取的内存中的数据。
测试3：测试8次，第i次写入4组共16个字的数据，index为i，后两组数据将覆盖前两组数据，然后读取这四组数据，这四次都会触发cache的替换过程，将比较这8次是否正确地进行了替换。
